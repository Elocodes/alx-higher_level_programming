## Mutable and Immutable objects in Python

Project contains text files that answer to use cases of objects in python. For instance,
if "b" is the clone of a list "a", do a and b point to the same object?
